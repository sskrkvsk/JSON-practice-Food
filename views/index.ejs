<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <%if (!locals.choice) {%>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <%} else if (choice == "chicken") {%>
      <h2><%= data[0].name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%= protein(0) %></li>
        <li><%= salsa(0) %></li>
        <% for (let i = 0; i < whatToplings(0).length; i++) { %>
        <li><%= whatToplings(0)[i] %></li>
        <% }%>
      </ul>
      <%} else if (choice == "beef") {%>
      <h2><%= data[1].name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%= protein(1) %></li>
        <li><%= salsa(1) %></li>
        <% for (let i = 0; i < whatToplings(0).length; i++) { %>
        <li><%= whatToplings(1)[i] %></li>
        <% }%>
      </ul>
      <%} else if (choice == "fish") { %>
      <h2><%= data[2].name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%= protein(2) %></li>
        <li><%= salsa(2) %></li>
        <% for (let i = 0; i < whatToplings(0).length; i++) { %>
        <li><%= whatToplings(2)[i] %></li>
        <% }%>
      </ul>
      <%}%>

      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    </div>
  </body>
</html>
